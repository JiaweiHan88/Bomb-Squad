# Deferred Work

## Deferred from: code review of story-1.1 (2026-06-10)

- ~~**`@bomb-squad/shared` entrypoint strategy**~~ — **RESOLVED in Story 1.2.** Updated `exports` to `{ "types": "./src/index.ts", "import": "./src/index.ts", "default": "./src/index.ts" }`. Added `.js` extensions to all relative imports inside `packages/shared` (NodeNext-compatible, Bundler also accepts them). Both `apps/client` and `apps/server` declared `"@bomb-squad/shared": "workspace:*"` and cross-workspace `import type` resolved cleanly under all three tsconfigs.
- **`@types/node` for client Vite config typecheck** — `apps/client/tsconfig.json` includes `vite.config.ts` in the typecheck graph without `@types/node`. Passes today (no Node globals used); add `@types/node` + a `tsconfig.node.json` split when the Vite config starts using `path`/`process`/etc.

## Deferred from: code review of story-1.2 (2026-06-11)

- **`PAUSED`/`RESUMED` event payloads carry no `TimerState`** — `PauseResumePayload` is only `{ reason: string }`, partly redundant with `TIMER_UPDATE`. Decide whether pause/resume events should bundle the frozen `TimerState` when the timer/pause story (1.4+) implements the clock. Deferred — depends on consuming story.
- **Referential integrity between `PlayerInfo.teamId` and `TeamState.relayOrder` is unmodeled** — a player's `teamId` can disagree with which team's `relayOrder` lists them. Validate at runtime in the session-state story rather than encoding in the pure type. Deferred — runtime validation concern.

## Deferred from: code review of story-1.3 (2026-06-11)

- **`@bomb-squad/shared` exports point at `.ts` source, not built `dist`** (`packages/shared/package.json:5-11`) — works today (client bundles via Vite; server uses only `import type`, erased at compile), but breaks the moment the tsc-compiled server imports a runtime value (`hash`, `makeSeededRng`) since Node cannot execute the `.ts`. Deliberate Story 1.2 decision, validated only for `import type`. Settle the runtime-value consumption strategy (built dist + conditional exports / TS project references / bundler) in Story 1.4 (server bootstrap). Deferred — depends on the first server runtime import of shared.
- **`StrikeCount` admits `3` as a steady-state value though third strike = explosion** (`packages/shared/src/types/bomb.ts`) — `BombState.strikes: 0|1|2|3` can rest at `3`. Whether the type or reducer should forbid a resting `3` is a modeling question for the strike/timer reducer. Deferred — resolve in Story 8.4 (server-authoritative timer and strike escalation).

## Deferred from: code review of story-1.4 (2026-06-11)

- **CORS `origin: true` reflects any origin** (`apps/server/src/index.ts:37`) — open cross-origin posture, but exactly what Story 1.4 Task 4 specifies for the bootstrap. Tighten to an allowlist in a later auth/deployment story. Deferred — spec-sanctioned for this story.
- **No graceful-shutdown hang-timeout; signal during in-flight `listen()` can exit non-zero** (`apps/server/src/index.ts:51-70`) — if `io.close`'s callback never fires the process wedges until SIGKILL; a signal arriving before `ready()`/`listen()` completes closes a not-yet-listening server and may reject into `exit(1)`. Add a forced-exit timer and gate the signal handlers on listen-complete when shutdown robustness matters. Deferred — low-likelihood in current scope.
- **Config shape and numeric errors are not aggregated into one boot failure** (`apps/server/src/config/env.ts:64-86`) — `parseEnv` throws on shape issues before numeric validation runs, so a missing key + bad PORT surfaces only the shape error first (two-round fixing), contradicting the function's own "see every problem at once" comment. Deferred — minor operator UX.
- ~~**`HealthRegistry.runAll()` doesn't normalize a malformed resolved value; `register()` silently replaces a duplicate name**~~ — **RESOLVED in Story 1.5.** `runAll()` now coerces `undefined`/`null`/non-`{ok:boolean}` probe results to `{ ok: false, detail: 'malformed readiness result' }`. `register()` now throws on a duplicate name (fail-loud). Both covered by unit tests in `src/__tests__/health.test.ts`.

## Deferred from: code review of story-1.5 (2026-06-12)

- **Signal during blocking `await redisClient.connect()` is unguarded** (`apps/server/src/index.ts:59-63,119-120`) — SIGINT/SIGTERM handlers are registered *after* the initial Redis connect, which can block for the OS TCP-connect timeout against an unreachable host. A signal in that window kills the process with no graceful close (Redis client + pg pool leak). Story 1.5 widened the pre-existing "signal before `listen()`" window (see Story 1.4 deferral above) by inserting the blocking connect. Fix alongside that item — register handlers earlier *and* gate them on listen-complete, or make the initial connect non-blocking. Deferred — low-likelihood in current scope.
- **Adapter error events bypass the pino logger and can spam on reconnect** (`apps/server/src/state/index.ts:790-792`, `apps/server/src/persistence/index.ts:558-560`) — `client.on('error')`/`pool.on('error')` use `console.error`, not `fastify.log`, so the output is unstructured and out-of-band; a permanently-down store produces an unbounded reconnect-error stream (each attempt logs). Crash is already prevented (handlers attached). Inject the Fastify logger into `connectRedis`/`connectPostgres` and consider a reconnect-log rate cap / `retryStrategy` backoff. Deferred — operational polish, non-fatal.
- **Redis key-builders don't validate `sessionId`/`teamId`** (`apps/server/src/state/keys.ts`) — raw interpolation means an ID containing `:` or an empty string silently produces a colliding/malformed key. Spec-sanctioned deferral (story Dev Notes: "keep IDs from containing `:` is a later-story concern; join codes are alphanumeric"). Revisit when ID generation/validation lands. Deferred — spec-sanctioned.

## Deferred from: code review of story-1.6 (2026-06-12)

- **No runtime guard on module-reducer output contract** (`apps/server/src/reducers/bombReducer.ts:9-14`) — `applyModuleResult` trusts the `ModuleState` a module reducer returns. Because `ModuleReducer = Reducer<ModuleState<unknown>, unknown>` erases the per-module type, a buggy reducer can return an out-of-contract `status` (anything other than `armed|solved|struck` is silently treated as not-solved/not-struck) or a different `moduleId`, permanently rebinding the slot to another (possibly unregistered) reducer. No validation, no test. Deferred — module reducers are first-party and individually typed; add a defensive guard (stable `moduleId` + legal `status`) when the Epic 5 module plugin contract is built.

## Deferred from: code review of 1-8-docker-compose-stack-and-smoke-test (2026-06-12)

- **`vite preview` has no SPA fallback** (`apps/client/Dockerfile`) — unknown paths are not rewritten to `index.html`, so deep links / refresh on client-side routes return 404 through Caddy's catch-all proxy. Not triggered yet: the client (story 1.7) is single-page with no router. Revisit when client-side routing lands — either switch to Caddy-served static with `try_files`, or add a preview-server fallback.
- **App containers run as root** (`apps/server/Dockerfile`, `apps/client/Dockerfile`) — no `USER` directive in either runtime stage. Belongs in the deferred `docker-compose.prod.yml` hardening overlay (resource limits, read-only FS, non-root user). Deferred — prod hardening out of scope for the current milestone.
- **`docker-compose.prod.yml` production overlay** (`docs/deployment.md` §Deferred) — production hardening overlay: resource limits, read-only filesystems, non-root `USER` in app images, removal of debug port publications. Out of scope for the current milestone; this entry is the citation target for `docs/deployment.md`.
- **Production WebRTC reachability — LiveKit signaling has no `wss://` path** (`Caddyfile`, `docker-compose.yml` livekit service) — Caddy routes only `/socket.io/*`, `/health`, and the SPA; LiveKit signaling is published as plain `ws://:7880`. A page served over `https://` cannot open an insecure WebSocket (mixed content), so voice cannot connect off-localhost as wired. Resolve in the LiveKit voice stories: add a Caddy TLS route for LiveKit signaling (or enable LiveKit TLS) before any non-localhost voice test. Deferred 2026-06-12 (code review of story 1.8) — no voice client code exists yet.
- **Production WebRTC reachability — coturn lacks `--external-ip`** (`docker-compose.yml` coturn service) — without it, XOR-RELAYED-ADDRESS advertises the container's internal RFC1918 address, so external clients send relay traffic to an unroutable IP even though the relay range is published. Resolve in the voice stories: env-driven `--external-ip=${TURN_EXTERNAL_IP}` (host public IP, deploy-time value). Deferred 2026-06-12 (code review of story 1.8) — needs deployment-specific input and a real voice path to verify against.

## Deferred from: code review of 2-1-design-tokens-ui-shell-and-state-patterns (2026-06-12)

- Disconnected/failed connection shows "Connecting…" indefinitely with no failure or retry affordance (`apps/client/src/App.tsx`) — matches story 2.1 spec (loading screen covers both `disconnected` and `connecting`; socket.io auto-reconnects), but a dead server is indistinguishable from a slow connect. Add error-state UX (e.g. "Reconnecting…" / failure copy + retry) in a later Epic 2 story.
- Fonts load via render-blocking third-party CDN `@import`s — Google Fonts + jsDelivr (`apps/client/src/index.css:1-2`). Offline/firewalled users get fallback fonts; GDPR exposure from Google Fonts; DSEG7 is dead weight until Epic 4. Self-host all five families before production.
- `aria-live="polite"` on a freshly mounted LoadingScreen may never announce its status to screen readers — live regions announce changes, not initial content (`apps/client/src/ui/LoadingScreen.tsx`). Minor a11y polish; revisit with the accessibility-floor pass.
- No component tests for ConfirmButton's two-step state machine or PlatformGate precedence — story 2.1 test scope was pure logic only per project testing rules; revisit when component-test / visual-regression infra (Playwright) lands.
- iPad desktop-UA bypasses the mobile bounce (`apps/client/src/ui/platform.ts`) — iPadOS 13+ Safari reports a `Macintosh` UA, so `isMobileUA` never matches. Accepted gap (decision 2026-06-12): an iPad Pro at 1366×1024 passes the viewport gate and may be usable; revisit with `maxTouchPoints` heuristic if iPad reports surface.

## Deferred from: code review of story 1-7 (2026-06-12)

- `transports: ['websocket']` has no polling fallback (`apps/client/src/net/socket.ts:10`) — clients behind proxies/firewalls that block the WS upgrade can never connect. Spec-prescribed in story 1.7 Task 2; revisit during NAT/firewall testing (voice epic, per project-context "test behind symmetric NAT").
- `applyModuleUpdate` doesn't verify the documented `moduleId` invariant (`apps/client/src/store/gameStore.ts`) — shared contract says `modules[moduleIndex].moduleId === state.moduleId`; a mismatched payload would overwrite the wrong module. Server-guaranteed today; add a guard when MODULE_UPDATE is actually emitted (Epic 2/3).
- `ERROR` with `recoverable: false` has no fatal-path handling (`apps/client/src/net/bindServerEvents.ts`) — both branches just `console.error`. Handler is a spec-sanctioned stub; real error UX lands with Epic 2+.
- Reconnect leaves stale `session`/`bomb`/`timer` with no resync (`apps/client/src/net/bindServerEvents.ts`) — after drop+reconnect the UI shows "connected" against dead-connection state. Resync arrives when the server emits SESSION_STATE on (re)join in Epic 2.

## Deferred from: code review of story-2.2 (2026-06-12)

- Facilitator identity persisted as the ephemeral `socket.id` (`apps/server/src/session/createSession.ts`, `handlers/sessionHandlers.ts`) — `players` is keyed by `socket.id`, which Socket.IO rotates on every (re)connection. Functional for V1 single-process with no reattach (the spec deliberately defers reattach and accepts losing the lobby on refresh), but the session-reattach story must introduce a stable, durable player id before any reconnect/rejoin flow, or the facilitator will no longer match their own stored player record.

## Deferred from: code review of story-2.3 (2026-06-12)

- Reconnect breaks socket.id-as-player-identity in the join/roster path (`apps/server/src/handlers/sessionHandlers.ts`, `apps/client/src/ui/Lobby.tsx`) — extends the 2.2 socket.id deferral to the joiner side. Socket.IO rotates `socket.id` on every reconnect, so: the idempotent-rejoin guard (`state.players[socket.id]`) misses and adds a duplicate roster entry; the stale entry is never removed (there is no `disconnect` handler anywhere in the change); the Lobby "You" tag (`player.playerId === getSocket().id`) silently disappears; and ghost entries count toward `MAX_PLAYERS`, so a flapping client can falsely exhaust capacity. The session-reattach story must land a stable durable player id and a disconnect/cleanup path before any reconnect/rejoin flow.
- Concurrent `SESSION_JOIN` load-modify-store race (`apps/server/src/handlers/sessionHandlers.ts`) — two joins interleaving load→modify→store can drop a player (last-write-wins), and two racers at 15-player occupancy can both pass the `>= MAX_PLAYERS` guard and push the roster to 17. Explicitly accepted by Task 3 ("Known accepted race") and commented in code; single-process, human-speed-lobby V1 assumption. Revisit with Redis-level serialization (WATCH/Lua) when/if it becomes load-bearing — Story 2.6 (capacity + join-window) is the natural home.
- Display-name length validated by UTF-16 `.length`, not grapheme/codepoint count, with no NFC normalization (`apps/server/src/handlers/sessionHandlers.ts` validator, `apps/client/src/ui/joinCode.ts` `isJoinReady`, client `maxLength={24}`) — astral/emoji names hit the 1–24 bound at the wrong visible length; `maxLength={24}` can truncate a paste mid-surrogate (lone surrogate then trimmed and sent); a single zero-width/combining char passes the 1-char floor as an invisible/garbled roster row. Mirrors the existing `createSession` pattern; acceptable for V1. Revisit with an i18n/input-hardening pass (grapheme segmentation + NFC + printable-char floor).

## Deferred from: code review of 2-4-team-and-per-player-role-assignment (2026-06-12)

- Redis load-modify-store lost-update race on concurrent `TEAM_ASSIGN` (`apps/server/src/handlers/sessionHandlers.ts`) — two rapid facilitator assignments read the same base state, each computes `next`, and the later `setJSON` clobbers the earlier (lost assignment / dropped relayOrder append). Explicitly accepted V1 (single-process, human-speed lobby) and commented in code; same posture as the SESSION_JOIN race (2.3 deferral). Revisit with Redis-level serialization (WATCH/Lua) when concurrency becomes load-bearing — Story 2.6 (capacity + join-window) is the natural home.
- `TeamState.currentDefuserIndex` not re-clamped when a player leaves a team's `relayOrder` (`apps/server/src/session/assignTeam.ts`) — the index is carried over via `...previous` unchanged, so after a cross-team move it can point past the shortened `relayOrder` or at a different player. Harmless in this story (lobby phase keeps the index at 0; no round has run), but a latent invariant break once Epic 8 lets assignment happen between rounds. Resolve when Epic 8 owns rotation/index mechanics (revisit the index-stays-0 assumption noted in `assignTeam.ts`).
- `relayOrder` ↔ `PlayerInfo.teamId` divergence is not self-healed (`apps/server/src/session/assignTeam.ts`) — `assignPlayerToTeam` trusts `player.teamId` as the sole source of truth for which team's `relayOrder` to clean. If a player's `teamId` ever disagrees with the team whose `relayOrder` actually lists them (inconsistent seed / partial prior write), a subsequent move leaves a ghost `relayOrder` entry in the other team that is never reconciled. Not reachable via 2.4's own writes (they stay consistent); this is the runtime-owner recurrence of the 1.2-deferred referential-integrity item. Add a reconciliation/validation pass if a non-2.4 mutation path is ever introduced.
## Deferred from: code review of 4-1-3d-bomb-scene-and-camera-rig (2026-06-12)

- **Bomb-scene focus state not auto-cleared on remount or module-count shrink** (`apps/client/src/scenes/BombScene.tsx` CameraRig) — `uiStore.activeModuleIndex` persists across a `BombScene` remount and is not validated against the current slot count. A stale index leaves the camera framed on a now-absent module (`slots.find` returns undefined → effect no-ops; ESC recovers). Harmless in 4.1 (no remount path, store defaults to null), but the round-lifecycle stories (4.6/4.7) that mount the scene from session state must clear/validate focus on round transitions.

## Deferred from: implementation of 4-2-chassis-and-bomb-metadata-rendering (2026-06-12)

- **Camera can clip through the chassis ends at max wheel-zoom** (`apps/client/src/scenes/BombScene.tsx` CameraRig `MIN_DISTANCE = 1.2`) — the distance clamp is smaller than the chassis half-width (1.5), so wheel-zooming while orbited to a ±x end face pushes the camera inside the geometry (observed in the 4.2 headless smoke). Pre-existing 4.1 clamp characteristic, newly observable now that the end faces carry the serial sticker; front/top/bottom faces are safe (half-extents < 1.2). The serial is fully legible well before the clamp, so AC2 of 4.2 holds. Fixing it means raising `MIN_DISTANCE` above ~1.8 (the rib corner radius), which clamps the 4.1 focus-dolly pose (`FOCUS_DISTANCE = 1.6`) — a camera-feel decision belonging to the bomb-view polish/hardening stories (4.7/10.2), not a metadata-rendering change.

## Deferred from: code review of 4-2-chassis-and-bomb-metadata-rendering (2026-06-12)

- **Battery cells/tray spill off the top face at batteryCount ≥17** (`apps/client/src/scenes/chassis.ts`) — beyond the documented ~12 max, so unreachable in the dev harness; `batteryCount` is an unbounded `number` with no clamp, so a generator emitting a high count would silently place geometry off-chassis. Add a clamp/guard when server-side `BombContext` generation lands (Story 8.2). Deferred — beyond current realistic domain.
- **Manual-smoke results (a)–(g), incl. AC2 <10 s timing, are headless-only and unverifiable from code** — recommend a human interactive browser pass to confirm sticker size at preferred zoom and indicator glow intensity (the implementer's own recommendation). Deferred — human verification, not a code defect.

## Deferred from: code review of 4-3-module-slots-and-solve-leds (2026-06-12)

- **A module resting permanently at `'struck'` renders as dim-red (armed) after the 600ms flash** (`apps/client/src/scenes/moduleLed.ts`) — the LED is edge-triggered off the armed→struck transition and reverts to the armed visual once the 600ms window expires; a `'struck'` status with no active flash maps to armed. If Epic 8 / a 4.7 snapshot ever delivers a module stuck at `'struck'` (no following `'armed'` roll-up), that module is visually indistinguishable from an armed one. Correct per the transient-`'struck'` contract for 4.3; Story 8.4 should make the transient-struck broadcast an explicit server contract (already flagged in this story's Dev Notes / Completion Notes).

## Deferred from: code review of story 5-2 (2026-06-12)

- **`MANUAL_NAVIGATE` expert-authority check keys on the transient `socket.id`** (`apps/server/src/handlers/manualHandlers.ts:90`) — `state.players[socket.id]?.role !== 'expert'` resolves the sender by `socket.id`, which Socket.IO rotates on every (re)connection. An Expert who reconnects with a new socket id is silently treated as a non-expert and their navigation stops being published (the spectator mirror, Story 9.4, freezes on the last good position). Not introduced here — it follows the exact established convention (`sessionHandlers.ts:417` facilitator check, `players` keyed by `socket.id`) and is the manual-relay recurrence of the 2.2/2.3 socket-id-as-identity deferrals. Resolved wholesale when the session-reattach story introduces a stable durable player id; the relay check should switch to that id at the same time. Deferred — pre-existing, codebase-wide.

## Deferred from: manual verification of story 8-3 (2026-06-13)

- **Lobby role pick (defuser/expert) is overridden by rotation at `ROUND_START`** (`apps/client/src/ui/Lobby.tsx` role select; `apps/server/src/session/startRound.ts:49-58`) — a player or facilitator can choose `defuser`/`expert` in the lobby, but `startRound` force-assigns the defuser from `relayOrder[currentDefuserIndex]` and demotes any other `defuser` on the team to `expert`, so the lobby choice is non-binding for the defuser/expert distinction (only participant-vs-spectator survives). Surfaced by Jay during 8.3 manual verification. **Decision: defer the role-pick semantics to Story 8.6**, where rotation-advance mechanics land — decide there among: (a) lobby chooses participant-vs-spectator only, (b) lobby pick seeds round 1's defuser then rotates, or (c) keep rotation authority and relabel the control as "starting role / rotation order". No logic change in 8.3.
- **`cancelPreparation` always returns to `'lobby'`** (`apps/server/src/session/cancelPreparation.ts`) — correct while prep is only reachable from `'lobby'` in 8.3. When `'between-rounds' → preparation` becomes reachable (Stories 8.5/8.6), cancel must restore the originating phase rather than hard-coding `'lobby'`, and the `roundNumber` decrement must be reconciled with between-rounds bookkeeping.

## Deferred from: code review of story-8-3 (2026-06-13)

- **Stale `defuser` role on a skipped team after `ROUND_START`** (`apps/server/src/session/startRound.ts`) — the role-flip loop only iterates over the `defusers` map (teams with a resolved pick); a team skipped because its `relayOrder` pick is absent from `players` is not cleaned up. Players on that team who held `role: 'defuser'` from a prior assignment retain it in the emitted `SESSION_STATE`, so `ActiveRound.tsx` routes them to the bomb surface even though they are not in `round.defusers`. Requires the known `relayOrder`↔`players` divergence to materialise (player disconnects after prep opens). Deferred — pre-existing divergence tracked in 2.4 deferral above; resolve when session-reattach / relayOrder integrity lands.
## Deferred from: code review of 4-4-diegetic-timer-lcd-with-client-extrapolation (2026-06-12)

- **Server-clock offset not reset on disconnect/reconnect** (`apps/client/src/net/serverClock.ts`) — `clockOffsetMs` is a process-global module variable cleared only by the test-only `resetClockOffsetForTest()`. The `bindServerEvents` `off()` teardown removes socket listeners but never clears the offset, so a reconnect to a different server epoch (or after a client clock adjustment) keeps the previous connection's offset until the first new running `TIMER_UPDATE` arrives — momentarily biasing `serverNow()`, potentially *ahead* of the server (the phantom-expiry direction AC3 guards against). Unreachable in the single-connection dev harness (offset 0, same origin). Defer to Story 8.4 when the real server timer emitter lands — reconnect/RTT/smoothing semantics are explicitly that story's scope (the file's own doc comment defers to it).
- **Housing↔feature overlap clearance proven for single-row layouts only** (`apps/client/src/scenes/timerLcd.ts` `TIMER_HOUSING_FOOTPRINT`) — the top-band LCD housing clears the indicator zone and battery tray for ≤6 indicators / ≤8 batteries (single-row), enforced by the overlap test in `__tests__/timerLcd.test.ts`. Two-row indicator/battery layouts would consume the central band the housing occupies and collide. Dev context (2/2/2) is well inside the envelope. Defer to Story 8.2 when `BombContext` generation ranges land — if they exceed the envelope the band must be renegotiated; the overlap test fails loudly to catch it. Already noted in the story's Completion Notes ("Note for 8.2").

## Deferred from: implementation of 4-5-strike-indicator-and-strike-roll-up (2026-06-13)

- **Timer LCD glow reduces digit readability as strikes accumulate** (`apps/client/src/scenes/TimerLcd.tsx` glow plane, `apps/client/src/scenes/timerLcd.ts` `lcdGlowIntensity`) — Jay's interactive observation during 4.5 verification: the additive glow plane sits in front of the digits (a deliberate 4.4 choice — behind the opaque plate it was invisible), so the DESIGN-mandated "+20% glow intensity per strike" progressively washes the digits at 2–3 strikes, hurting the exact readability the 84px ruling exists for. Both values are literal DESIGN.md specs (`componentSpec.timer` glow + pulseOnStrike), so retuning is a design decision, not a bug fix: candidates are lowering `GLOW_OPACITY_SCALE`, capping the strike-adjusted base, or moving the glow to a rim/outline (troika `outlineBlur`) that brightens around rather than over the digits. Belongs to the UX/polish pass (10.2 frame-budget/feel hardening or a DESIGN.md revision), not to 4.5 (strike LEDs only) — flagged here so it isn't lost.

## Deferred from: code review of 4-5-strike-indicator-and-strike-roll-up (2026-06-13)

- **Strike indicator shows the wrong dot floor on a `STRIKE`-before-`BOMB_INIT` race** (`apps/client/src/scenes/StrikeIndicator.tsx`, `apps/client/src/store/gameStore.ts` `setStrike`) — if a `STRIKE` event arrives before `BOMB_INIT`, `setStrike` drops the strike count (applies only the timer, with a desync warn) because `bomb` is null; the `s.bomb?.strikes ?? 0` selector then renders 0 dots while the server's authoritative count is ≥1. Pre-existing store ordering guard, not introduced by 4.5; unreachable in the dev harness (the `if (!module) return` guard guarantees a bomb exists before any strike). Defer to Story 8.4 when the real server `STRIKE` emitter and reconnect/ordering semantics land — same scope as the deferred server-clock-offset item above.
- **Strike-housing↔feature overlap proven only at the single-row envelope** (`apps/client/src/scenes/strikeIndicator.ts` `STRIKE_HOUSING_FOOTPRINT`, `__tests__/strikeIndicator.test.ts`) — the footprint tests assert clearance against the indicator zone and battery tray at `≤6` indicators / `≤8` batteries (the single-row corner), and inherit the timer housing's z-band verbatim. They do not exercise the two-row transition edge (9th battery / 7th indicator) where a second row consumes the band — the same gap already deferred to 8.2 for the timer housing, now extended to the new strike housing. A `BombContext` range widening past the single-row envelope must renegotiate both housings together; the overlap tests fail loudly to catch it. Defer to Story 8.2.
## Deferred from: implementation of 5-3-wires-module-walking-skeleton (2026-06-13)

- **Module solve chime (epic 5.3 AC phrase "LED green, solve chime")** — no audio infrastructure exists; EXPERIENCE.md specifies module-typed chime pitches ("distinct pitch per module type"), which is an audio-design concern owned by Story 10.1 (game-state SFX). The in-scope solve confirmation is the 4.3 bay LED flipping green from `status: 'solved'`. Resolve in Story 10.1.
- **Production MODULE_INTERACT dispatch backend** — `apps/client/src/modules/dispatch.ts` said "Story 5.3 / Epic 8 installs it"; resolved in 5.3 as **Epic 8**: the server has no bomb lifecycle (no BOMB_INIT, no round state in Redis) until 8.2/8.3, so a handler would have nothing to load or reduce against. The sandbox's local backend remains the sanctioned dev path; Epic 8 swaps it via `setModuleActionDispatch`.

## Deferred from: code review of story 5-3 (2026-06-13)

- **`solutionIndex` is server-authoritative answer data that must be stripped before it crosses to clients** (`packages/shared/src/modules/wires/{types,generate}.ts`) — `WiresState.solutionIndex` is baked in at generate time because the reducer signature carries no `BombContext`. In a co-op information-asymmetry game the Defuser must not see the answer; once bomb/module state is broadcast, this field is a literal cheat value in the client payload. **Verified absent from all three in-flight Epic 8 worktrees (8-2, 8-3-8-4) — none currently strips it, because none broadcasts bomb state yet:** 8.2 only *generates + persists to Redis* (no client emission); 8.3 emits `SESSION_STATE` but leaves an **unfilled seam** at `apps/server/src/handlers/sessionHandlers.ts:678-680` (`// Story 8.2: per-team bomb generation slots in here … BOMB_INIT broadcasts to the team rooms`). The leak materializes the moment that seam is filled (the 8.2↔8.3 merge wiring adds `initializeRoundBombs` + the `BOMB_INIT` emit), and again in the future `MODULE_UPDATE` handler. **Owner: whoever fills the `BOMB_INIT` seam at `sessionHandlers.ts:678` must route client-bound bomb state through a solution-stripping projection (a shared `toClientBombState` / per-module client-safe `data` projection), and the `MODULE_UPDATE` handler must reuse it.** Field is provably derivable from the public manual rules + serial, so this is correctness/anti-cheat hygiene, not a secret-key leak — but the answer must not ship pre-computed. Flagged by the 5.3 implementer (story Completion Notes L159) and confirmed unhandled by this review.

## Deferred from: code review of story 8-4 (2026-06-13)

- **`remainingMs` has no upper clamp** (`apps/server/src/timer/timerCore.ts`) — with `now < startedAt` (clock skew between the scheduler's `clock()` and the handler's stamp, or a client `serverNow()` landing before a fresh `startedAt`), `remaining` exceeds `remainingAtStart` and the displayed clock shows more than the configured duration. Expiry authority is unaffected (`expiryInstant` is computed from `startedAt`). Cheap one-line `Math.min(.., remainingAtStart)` clamp; deferred as display-only.
- **`elapsedMs` at expiry is the configured `timerMs`, not real wall-clock** (`apps/server/src/timer/onTimerExpired.ts:33`) — once strikes accelerate the segment, real elapsed time at timeout is less than `timerMs`. Settled in 8.4 by design decision 6 (record *displayed* elapsed), but **Story 8.5** must reconcile real-vs-displayed before summing `BOMB_EXPLODED.elapsedMs` into `TeamState.cumulativeTimeMs`, or per-round time will over-count under strikes. Owner: 8.5 round resolution.
- **`escalateOnStrike` is not idempotent against duplicate strike delivery** (`apps/server/src/timer/escalateOnStrike.ts`) — a re-delivered strike-1 double-compounds the multiplier (×1.25 → ×1.5625). The function's header contract says "pass the NEW absolute strike total"; deliver-once is owned by the future server-side `MODULE_INTERACT` handler (Story 8.2 / Epic-5 interaction wiring), which does not exist yet. Owner: whoever wires `MODULE_INTERACT → bombReducer → escalateOnStrike`.
- **`cancel`/`cancelSession` are unwired for normal round-end / defuse** (`apps/server/src/timer/timerScheduler.ts`) — only `dispose` (shutdown) and re-`arm` clear wakes today; a defuse/early-resolution path that cancels a live timer is **Story 8.5**. Contained for now because `fire` reload-no-ops on a deleted key. Owner: 8.5.
- **`expiryInstant`/`arm` go non-finite on a corrupt `remainingAtStart`** (`apps/server/src/timer/timerCore.ts`) — a hand-edited/corrupt Redis `Infinity` flows through `expiryInstant` → `delay = Infinity` → `setTimeout` fires immediately → reload no-ops forever (unkillable, un-expiring timer). A cheap `Number.isFinite` guard closes it; deferred because it requires already-corrupted state.

/** Pure decision rules + release table — shared with the server and the manual (re-export). */
export {
  BUTTON_RULES,
  decideButton,
  releaseDigitFor,
  STRIP_RELEASE_DIGIT,
  type ButtonRule,
  type ButtonDecision,
} from '@bomb-squad/shared';
